import { NgInitPage } from './app.po';

describe('ng-parts App', () => {
  let page: NgInitPage;

  beforeEach(() => {
    page = new NgInitPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toContain('app works!');
  });
});
